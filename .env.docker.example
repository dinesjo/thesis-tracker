# Core app
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Auth.js
AUTH_SECRET=replace-with-a-long-random-secret
# For production behind a domain, set both AUTH_URL and NEXT_PUBLIC_SITE_URL to https://your-domain.example
AUTH_URL=http://localhost:3000
AUTH_TRUST_HOST=true

# SQLite path inside container.
# Note: docker-compose.yml sets this explicitly to /app/data/thesis-tracker.db.
DATABASE_FILE=/app/data/thesis-tracker.db
